1. Setup ide
  + .prettierrc
  + .vscode/settings.json
  + .env.local
    PORT=3000
  + config postCss sorter extension
2. Install lib
  + customsize-cra
  + babel-plugin-module-resolver
  + Sass, normalize.css
  + react-router-dom
  + classnames
  + PropTypes
3. Setup project
  + Reset css
  + Fonts embedded, color origin
  + Folder division
  + Create repo github
4. Header component
  + Logo
  + Search component
  + Button component
  + Search tooltip, more btn tooltip -> tooltip component/ tooltip lib